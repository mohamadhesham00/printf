#ifndef MAIN_H
#define MAIN_H
#include <stdarg.h>
#include <stdio.h>
#include <unistd.h>
int _putchar (char c);
void print_strings(const char *separator, const unsigned int n, ...);
int _printf(const char *format, ...);
#endif
